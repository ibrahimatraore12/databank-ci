# Design Decision Log - dataBank CI Customer 360

> *[Version française disponible : [decisions.md](decisions.md)]*

**Author:** Ibrahima TRAORÉ - Analytics Engineer

This document lists the important choices made during the project, why
they were made, and the other options that were not chosen.

Table format: Date | Decision | Reason | Alternative rejected

| Date | Decision | Reason | Alternative rejected |
|------|----------|--------|----------------------|
| 2026-07-14 | The disengagement score is computed in two steps: explicit business rules in Step 1, then a Machine Learning experiment on an approximate indicator in Step 2 | The source file contains no actually observed customer departure (see `docs/ml_problem_definition_en.md`). Presenting a Machine Learning score as certain truth would have been misleading | Train a Machine Learning model directly, without a business-rules step |
| 2026-07-14 | Generate a synthetic dataset (`is_synthetic=True`) to bring the volume from 140 to about 540 customers | With a single simple criterion, only 2 customers are affected (1.4%). Even with a broader criterion (25.0%), the sample stays small on 140 customers. A larger test sample is needed to compare models reliably | Train only on the 140 real customers and accept unreliable results |
| 2026-07-14 | The threshold used to define an "at-risk" customer is set at 2 out of 4 criteria (not tuned to reach a target rate in advance) | Measured on real data: a single criterion flags 70% of customers (far too many), 2 criteria give 25.0%, 3 criteria give only 0.7% (too few). The 2-criteria threshold is the best compromise, kept as-is rather than adjusted afterward to hit a specific number | Search for a threshold or weighting that would artificially produce 12-15% of customers affected |
| 2026-07-14 | All artificially generated data stays flagged `is_synthetic=True` in the final tables and in the dashboard | So no one can mistake generated data for real customer data | Silently mix real and synthetic data |
| 2026-07-14 | The revenue generated per customer (NBI) is estimated with the standard WAEMU formula, and flagged `estimated_nbi_flag=True` | The real accounting NBI figure doesn't exist in the source file | Don't compute any NBI at all |
| 2026-07-14 | The same starting value (`seed=42` and `random_state=42`) is used everywhere: dbt tests, the data split for Machine Learning, synthetic data generation | So that re-running the pipeline always produces exactly the same result | Let randomness vary on every run |
| 2026-07-14 | The business logic code doesn't use Python classes (object-oriented programming), only simple functions chained together | Easier and faster for an outside reviewer to read, consistent with the coding style set for this project | Use classic object-oriented modeling (`Customer`, `Pipeline` classes, etc.) |
| 2026-07-14 | No technical column name appears on the dashboard: everything goes through the `LABELS` file (`dashboard/components/ui.py`) and the `i18n/*.json` translation files | The dashboard is built for relationship managers, not technical staff | Display raw SQL or pandas column names directly |
| 2026-07-14 | The payment delay threshold (`days_past_due`) is set at 15 days to correct a loan's status in `stg_loans.sql` | Observed during exploratory analysis: loans actually classified `Watchlist` or `Delinquent` exceed this threshold, `Current` loans stay below it | Use the source file's status directly, without checking it against the real payment delay |
| 2026-07-16 | Files uploaded by a user are saved to a private Google Cloud Storage space, reloaded at every instance's startup (`src/storage_sync.py`) | Cloud Run doesn't keep anything permanently in memory: a file uploaded from the Administration tab would be lost on the very first restart without this external backup. Detail in `docs/architecture_en.md`, section 6 | Save only the source Excel file, or use a separately managed database |
| 2026-07-16 | A schema-version check (`config.DATA_SCHEMA_VERSION`) runs before any download from the GCS storage space | Without this check, a future structural change in dbt could be silently overwritten at startup by an old restored backup, causing cascading errors on any query using the new column | Download without any check and accept the incompatibility risk |
| 2026-07-16 | The `mlflow.db` file is cleanly copied using the `sqlite3.Connection.backup()` function to a temporary file before being sent to GCS, never copied directly byte for byte | MLflow can keep a connection open in the background; a raw copy would risk capturing the data at an inconsistent moment | Copy the `.db` file directly, without this clean backup step |
| 2026-07-17 | The Artefact visual identity (black `#0D0D0D` / orange `#FF4500`) is applied to all 9 pages through shared building blocks (`dashboard/components/ui.py`: banner, reading guide, section headers, indicator cards, alerts) | To keep visual consistency, with a single place to update instead of styling copied onto every page | Give each page its own style with hand-written CSS |
| 2026-07-17 | The "Commercial Opportunities" page is built from real historical acceptance rates, by offer type (`main_staging.stg_offers`), rather than a predicted probability for each customer | No model in this project computes a per-customer acceptance probability. Inventing one would have passed off an arbitrary estimate as a real prediction | Generate a per-customer probability using an unverified method |
| 2026-07-17 | On the "AI Assistant" page, the ROC curve and the drift indicator (PSI) are recomputed live, replaying the same data split every time (`seed=42`), with the result kept in memory for the session (`st.cache_resource`), instead of showing fixed values | Numbers frozen once and for all quietly become outdated as soon as the model or the data changes. The recomputation is verifiable and takes only a few hundred milliseconds | Show hard-coded numbers, or numbers copied from an old report |
| 2026-07-17 | Every page that shows alerts also displays a real positive signal, computed from the data (healthy portfolio, identified opportunities), not just risks | A dashboard that only shows problems gives a distorted picture of the situation and doesn't help show what's working well | Limit the page to risk alerts only |
| 2026-07-17 | The already-existing segment color palette was kept (Premier in purple, Affluent in blue, Mass in blue, Youth in pink), instead of the colors proposed in the Artefact visual charte | The blues proposed for Affluent and Mass (`#1A5276` / `#2980B9`) were visually too close to each other for a color-vision-deficient person (failed the color accessibility check) | Adopt the segment colors from the Artefact charte exactly as specified |
